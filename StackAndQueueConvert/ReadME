1.如何仅用栈结构实现队列结构？

思想：1.定义俩个栈
      2.push时栈1进
      3.pop时当栈1进栈2出，如果栈2有元素，则等栈2出完在从
      栈1进栈2；如果栈2为空，则从栈1进然后从栈2出。

2.如何仅用队列结构实现栈结构？

思想：1.定义两个LinkedList
      2.push时LinkedList1进
      3.pop时LinkedList1的元素进入LinkedList2，留最后一个赋值给一个变量然后返回.