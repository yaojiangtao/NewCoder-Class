随机快速排序：
随机选取一个数，将他与最后一个数进行交换，然后进行比较，将比它大的数放在后边，比它小的数放在前边。
实现：
1.定义四个变量，第一个L为当前指针，第二less为L-1设置为当前指针的前一个指针，第三个变量为more设置为数组的最后一个指针，第四个r为随机选取的数，
放在最后一个位置。
2.如果当前数比最后一个数小，则将当前数给less的后一个数（++less）,然后让L++
3.如果当前数比最后一个数大，则将当前数与more的前一个数（--more）交换
4.最后，将将放置在最后一个位置的数（选取的数）与more位置的数交换。
5.递归上面的步骤。