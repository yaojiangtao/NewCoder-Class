非递归遍历树
先序：1.使用栈，先将根节点入栈
      2.判栈空，如果栈不为空，先出栈并赋值给head，打印head
      3.然后压栈，先压右再压左


中序：1.新建栈，将根压栈，然后让左子树为head继续进栈
      2.当为空时，让它出栈并复制给head,打印，然后将右子树赋值给head,如果为空，继续出栈

后序：1.创建2个栈s1,s2
      2.将根节点给s1，s1出栈并赋值给head，将head压栈
      3.然后s1先压左再压右，所以每次进入head2的为先右后左
      循环输入
