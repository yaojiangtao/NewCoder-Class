介绍前缀树(try tree)
何为前缀树？ 如何生成前缀树？
例子： 一个字符串类型的数组arr1，另一个字符串类型的数组arr2。

思路：
    注意一定要将字符放到路径上，不要放到节点上。
 新建前缀树：
    如果一个字符没有，新建一个节点，若果有，则next看它的下个是否存在


 删除时：
        如果一个字符的path-1后为0，则它后面的只经过一次，将后面的直接赋值null
        
